# content of: tox.ini , put in same dir as setup.py
[tox]
envlist=flake8

[testenv:flake8]
deps =
    flake8
    -rdev-requirements.txt
commands=flake8 floating_ip_plugin
         flake8 --ignore=E401 libcloud_plugin_common
         flake8 security_group_plugin
         flake8 server_plugin
